import 'package:flutter/cupertino.dart';

//TODO: implementiraj Figma design ovdje, nakon sto dodas sve potrebne filmove u mocked_movies
// koristi ih ovdje i prikazi u listi kao na designu
class MoviesPage extends StatelessWidget {
  const MoviesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
